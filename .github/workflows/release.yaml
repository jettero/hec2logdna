
name: release

on:
  push:
    tags:
    - 'v*'
  workflow_dispatch:
    inputs: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v2
    - run: pwd; ls -al .github/workflows
    - uses: ./.github/workflows/pyinstaller.yaml
      # with:
        #  varname: valuehere
    - uses: ncipollo/release-action@v1
      with:
        artifacts: "sj2l"
        token: ${{ secrets.GITHUB_TOKEN }}
